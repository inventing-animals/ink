<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Ink.Demo.ViewModels"
             x:Class="Ink.Demo.Views.CalendarView"
             x:DataType="vm:CalendarViewModel">
  <!-- TODO: Subclass ink:Calendar and ink:CalendarDatePicker from Avalonia equivalents and use them here instead -->
  <ScrollViewer>
    <StackPanel Spacing="24" Margin="24">
      <TextBlock Text="Calendar" />

      <StackPanel Spacing="8">
        <TextBlock Text="Avalonia Calendar / CalendarDatePicker" />
        <TextBlock Text="Not yet themed - requires Ink template implementation."
                   Foreground="{DynamicResource Ink.Brush.Content.Muted}"
                   FontSize="12" />
      </StackPanel>

    </StackPanel>
  </ScrollViewer>
</UserControl>
